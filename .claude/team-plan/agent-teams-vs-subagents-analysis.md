# 🔬 技术分析：Agent Teams (Teammates) vs Subagents — 本质差异与混合搭配策略

> 分析日期：2026-02-12
> 分析来源：官方文档 (code.claude.com)、Codex 系统架构分析、社区实战文章、Anthropic 工程博客

---

## 一、一致观点（强信号）

### 1. 核心分水岭是"通信拓扑"，不是"是否并行"
- Subagents = **Hub-and-Spoke**（星型）：所有信息只能经主代理中转
- Teammates = **Peer-to-Peer + 广播 + 共享任务列表**：点对点直连 + 扇出通知 + 协作状态面
- 两者都支持并行执行，但 Teammates 支持工作者之间**实时交换发现、互相挑战、协商接口**

### 2. 进程模型完全不同
- Subagent ≈ **短生命周期子进程**（request/response 模式），执行完返回结果后退出
- Teammate ≈ **独立长生命周期进程**（supervisor + workers 模式），持续运行直到显式关闭

### 3. Token 成本差异显著
- Subagent：结果**压缩摘要**回传主上下文，主线程膨胀慢
- Teammate：每个维护**完整独立上下文**，成本 ≈ `lead + Σ(每个 teammate 完整会话)` + 消息开销
- 官方参考：Agent Teams plan mode 下约 **~7x 标准会话** token 级别

---

## 二、核心差异全景对比（8 个维度）

| 维度 | Subagents | Agent Teams (Teammates) |
|------|-----------|------------------------|
| **进程模型** | 父会话内派生的短生命周期子进程 | 完全独立的长生命周期 Claude Code 实例 |
| **通信拓扑** | 严格 Hub-and-Spoke，只能向主代理汇报 | 点对点消息 + 广播 + 共享任务列表 |
| **生命周期** | 即用即弃，完成后自动退出 | 显式管理：spawn → 工作 → shutdown → cleanup |
| **状态持久化** | 无状态（可通过 agent ID resume，但非常驻） | 有状态：持续会话、任务认领、消息投递 |
| **调度模型** | 中心化编排（父代理分派 + 回收） | 半去中心化：队长分派 **或** 队员自主认领 |
| **权限模型** | 继承 + **可细粒度限缩**（tools/permissionMode） | 继承 Lead 基线，spawn 时**不能差异化**，运行后可调 |
| **嵌套约束** | 不能再 spawn subagent（防递归） | 不能嵌套 team，单会话同时只管理一个 team |
| **用户交互** | 对用户**透明**（自动委派，只看最终结果） | 需用户**主动管理**（Shift+Up/Down、Split panes） |

---

## 三、被忽视的关键差异（不止"可以通信"）

### 🔑 差异 1：调度自主性
- Subagent：被动执行，主代理决定一切
- Teammate：**可以自主认领**共享任务列表中的可用任务（文件锁防竞争）

### 🔑 差异 2：用户可直接介入
- Subagent：用户无法中途干预工作过程
- Teammate：用户可以随时选中任意队员，直接发消息、重定向、进入其会话操作

### 🔑 差异 3：质量门控钩子
- Subagent：无专属生命周期钩子
- Teammate：`TeammateIdle`（空闲触发）和 `TaskCompleted`（完成触发），exit 2 可阻止

### 🔑 差异 4：可观测性
- Subagent：黑盒——只看最终返回的摘要
- Teammate：白盒——实时查看每个队员输出，共享任务列表

### 🔑 差异 5：Delegate Mode（纯协调模式）
- Subagent：主代理既编排又执行，职责混杂
- Teammate：队长可切换 Delegate Mode，严格限制为协调工具

### 🔑 差异 6：Plan Approval 流程
- Subagent：无审批机制
- Teammate：可要求队员先出计划，队长审批后才能实施

### 🔑 差异 7：错误恢复模型
- Subagent：失败时自动回退到主代理，低成本
- Teammate：失败后无法恢复已终止的 session，需重新 spawn

### 🔑 差异 8：配置持久性
- 自定义 Subagent：`.claude/agents/` 可版本控制、团队共享、跨会话复用
- Agent Teams：会话级别的临时结构，不持久化

---

## 四、最佳类比

| 类比 | Subagents | Agent Teams |
|------|-----------|-------------|
| 职场 | **外包承包商**：给需求，交付成果，不参与内部讨论 | **跨职能小队**：同一间屋子，互相讨论、挑战、对齐 |
| 编程 | **函数调用**（call → return） | **微服务集群**（独立进程 + 消息队列 + 共享状态） |
| 操作系统 | **同步/异步 Job Queue Worker** | **带共享工作队列的协作 Worker Pool（自调度）** |

---

## 五、场景决策框架

```
任务需求评估
  │
  ├── 工作者之间需要互相通信？
  │     ├── 否 → 用 Subagents（成本低、简单）
  │     └── 是 → 继续评估 ↓
  │
  ├── 需要接口契约实时对齐？（如 OAuth、支付、API 前后端联调）
  │     └── 是 → Agent Teams
  │
  ├── 需要多假设并行验证 + 互相挑战？
  │     └── 是 → Agent Teams
  │
  ├── 需要实时可观测每个工作者进度？
  │     └── 是 → Agent Teams
  │
  └── 预算敏感 / 任务独立 / 只关心结果？
        └── 是 → Subagents
```

---

## 六、混合搭配策略（深度探讨）

### 6.1 为什么需要混合？

单独使用任一机制都有局限：
- **纯 Subagents**：无法处理需要实时协商的跨模块集成
- **纯 Agent Teams**：简单任务上 Token 浪费严重（~7x 成本）

混合搭配 = **在对的阶段用对的工具**，平衡质量与成本。

### 6.2 三阶段混合模型

```
┌─────────────────────────────────────────────────────────┐
│                    完整功能开发流程                        │
│                                                         │
│  阶段 1：探索研究           阶段 2：协作实施              │
│  ┌──────────────┐         ┌──────────────────┐         │
│  │  Subagent A  │         │  ┌── Teammate 1 ──┐│        │
│  │  (代码库扫描)  │         │  │  后端 API     ││        │
│  ├──────────────┤  ──→   │  ├── Teammate 2 ──┤│        │
│  │  Subagent B  │         │  │  前端组件     ││        │
│  │  (文档检索)   │         │  ├── Teammate 3 ──┤│        │
│  ├──────────────┤         │  │  测试用例     ││        │
│  │  Subagent C  │         │  └────────────────┘│        │
│  │  (依赖分析)   │         │  (互相通信对齐接口)  │        │
│  └──────────────┘         └──────────────────┘         │
│        │                          │                     │
│        │摘要汇总到主代理            │                     │
│        ↓                          ↓                     │
│                    阶段 3：验证收尾                       │
│                   ┌──────────────┐                      │
│                   │  Subagent D  │                      │
│                   │  (集成测试)   │                      │
│                   ├──────────────┤                      │
│                   │  Subagent E  │                      │
│                   │  (安全审计)   │                      │
│                   ├──────────────┤                      │
│                   │  Subagent F  │                      │
│                   │  (性能检查)   │                      │
│                   └──────────────┘                      │
└─────────────────────────────────────────────────────────┘
```

| 阶段 | 使用 | 原因 | Token 效率 |
|------|------|------|-----------|
| **1. 探索研究** | Subagents | 独立任务、只需结果摘要、低耦合 | ★★★★★（最低成本） |
| **2. 协作实施** | Agent Teams | 跨模块接口需实时对齐、互相挑战 | ★★☆☆☆（成本高但返工少） |
| **3. 验证收尾** | Subagents | 独立验证、只需 pass/fail、无需协商 | ★★★★★（最低成本） |

### 6.3 四种典型混合模式

#### 模式 A：侦察-突击-扫尾

```
Subagents(侦察) → Agent Teams(突击) → Subagents(扫尾)
```

**场景**：全栈功能开发（如 OAuth 集成）
- 侦察：Subagent 扫描现有认证代码、数据库 schema、前端路由
- 突击：Teammates 分别负责后端 handler / 前端 UI / 测试，实时共享接口契约
- 扫尾：Subagent 跑集成测试 + 安全审计 + 性能检查

#### 模式 B：扇出-收敛

```
Agent Teams(扇出探索) → Subagent(收敛验证)
```

**场景**：复杂调试
- 扇出：Teammates 同时调查 3 个假说，互相挑战，收敛到根因
- 收敛：Subagent 按根因编写修复代码 + 回归测试

#### 模式 C：分层权限隔离

```
Agent Teams(决策层) + Subagents(执行层)
```

**场景**：高安全要求的变更
- 决策层：Teammates 讨论并达成架构共识（plan approval 机制）
- 执行层：Subagents 在受限权限模式下执行具体变更（permissionMode: plan）

#### 模式 D：渐进升级

```
Subagent(初始尝试) → 发现需要协作 → 升级到 Agent Teams
```

**场景**：不确定复杂度的任务
- 先用 Subagent 尝试，如果发现任务比预期复杂、需要跨模块协调
- 动态升级为 Agent Teams（Subagent 的探索结果作为 Team 的初始上下文）

### 6.4 混合搭配的操作要点

| 要点 | 说明 |
|------|------|
| **信息传递** | Subagent 阶段产出写入文件 → Agent Teams 阶段读取文件作为上下文 |
| **上下文隔离** | 阶段间通过文件而非会话上下文传递，避免上下文膨胀 |
| **成本控制** | 只在"需要互相通信"的阶段使用 Agent Teams，其余全用 Subagents |
| **会话管理** | Agent Teams 用完后 cleanup → 切回 Subagent 模式，保持环境干净 |
| **质量门控** | Agent Teams 阶段用 `TaskCompleted` hook 卡质量；Subagent 阶段用 `PostToolUse` hook 卡格式 |

### 6.5 CCG 架构中的混合实践

当前 CCG 命令体系已经体现了混合思路：

| CCG 命令 | 模式 | 说明 |
|----------|------|------|
| `ccg:team-research` | Subagents 式 | Codex + Gemini 并行探索，结果汇总到文件 |
| `ccg:team-plan` | 主代理 + Subagents | 读取研究文件，调用外部模型分析，产出计划 |
| `ccg:team-exec` | Agent Teams | spawn Builder teammates 并行实施 |
| `ccg:team-review` | Subagents 式 | 双模型交叉审查，结果汇总 |

这正是 **模式 A（侦察-突击-扫尾）** 的实例化。

---

## 七、核心结论

> **Subagent 和 Teammate 的根本差异不是"能否通信"这一个点，而是整个运行时模型的不同：进程生命周期、调度自主性、用户可介入性、质量门控、可观测性、权限粒度——这 6 个维度共同构成了两套截然不同的多代理范式。**
>
> - Subagent 优化的是**效率与成本**（短任务、低耦合、结果压缩）
> - Agent Teams 优化的是**跨角色一致性与集体决策质量**（长任务、高耦合、实时协作）
> - **最优实践 = 混合搭配**：在对的阶段用对的工具

---

## 参考来源

| 来源 | URL |
|------|-----|
| Agent Teams 官方文档 | https://code.claude.com/docs/en/agent-teams |
| Subagents 官方文档 | https://code.claude.com/docs/en/sub-agents |
| Hooks 文档 | https://code.claude.com/docs/en/hooks-guide |
| Agent Teams vs Subagents 实战 | https://charlesjones.dev/blog/claude-code-agent-teams-vs-subagents-parallel-development |
| Agent Teams 完整指南 | https://www.nxcode.io/resources/news/claude-agent-teams-parallel-ai-development-guide-2026 |
| Anthropic 多代理研究系统 | https://www.anthropic.com/engineering/multi-agent-research-system |
